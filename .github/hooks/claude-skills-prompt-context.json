{
  "version": 1,
  "hooks": {
    "UserPromptSubmit": [
      {
        "type": "command",
        "powershell": "$body = [Console]::In.ReadToEnd(); try { Invoke-WebRequest -UseBasicParsing -Uri 'http://127.0.0.1:4895/hook/prompt-context?agent=copilot&cwd=e%3A%5CSDV-NRW%5CSDV-PROJECT' -Method POST -Headers @{ 'Content-Type' = 'application/json' } -Body $body | Out-Null } catch { }",
        "timeoutSec": 8
      }
    ],
    "userPromptSubmitted": [
      {
        "type": "command",
        "powershell": "$body = [Console]::In.ReadToEnd(); try { Invoke-WebRequest -UseBasicParsing -Uri 'http://127.0.0.1:4895/hook/prompt-context?agent=copilot&cwd=e%3A%5CSDV-NRW%5CSDV-PROJECT' -Method POST -Headers @{ 'Content-Type' = 'application/json' } -Body $body | Out-Null } catch { }",
        "timeoutSec": 8
      }
    ]
  }
}
